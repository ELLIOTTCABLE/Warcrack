--[[
	Localization file for "Broken LFD"

]]--

local addonName, ns = ...
local AceLocale = LibStub("AceLocale-3.0")

do
	local L = AceLocale:NewLocale(addonName, "enUS", true)
	L[" /blfd NoSeconds => toggle showing seconds everywhere"] = true
	L[" /blfd ShowIdleText  => Toggle showing text when not doing something"] = true
	L[" /blfd ShowTooltipTime => Toggle showing time in queue in tooltip"] = true
	L[" /blfd default => reset to defaults"] = true
	L[" /blfd toggleicon => toggle minimap icon"] = true
	L["Broken Queue"] = true
	L["Broken_Queue Options"] = true
	L["Commands are not case sensitive"] = true
	L["Forming Group"] = true
	L["Hiding Minimap Icon"] = true
	L["Hiding Seconds"] = true
	L["Hiding Time in Queue on Tooltip"] = true
	L["Idle Text Disabled"] = true
	L["Idle Text Enabled"] = true
	L["In BG"] = true
	L["LFG Party"] = true
	L["Left In Dungeon"] = true
	L["Not Queued"] = true
	L["Queue Pop"] = true
	L["Queued"] = true
	L["Reset Options to Default"] = true
	L["Role Check"] = true
	L["Showing Minimap Icon"] = true
	L["Showing Seconds"] = true
	L["Showing Time in Queue on Tooltip"] = true
	L["Waiting"] = true
	L["Waiting for more"] = true
end

---Planning for the future...
do
	local L = AceLocale:NewLocale(addonName, "frFR")
	if L then

	end
end
do
	local L = AceLocale:NewLocale(addonName, "deDE")
	if L then

	end
end
do
	local L = AceLocale:NewLocale(addonName, "koKR")
	if L then

	end
end
do
	local L = AceLocale:NewLocale(addonName, "esMX")
	if L then

	end
end
do
	local L = AceLocale:NewLocale(addonName, "zhCN")
	if L then

	end
end
do
	local L = AceLocale:NewLocale(addonName, "esES")
	if L then

	end
end
do
	local L = AceLocale:NewLocale(addonName, "zhTW")
	if L then

	end
end
